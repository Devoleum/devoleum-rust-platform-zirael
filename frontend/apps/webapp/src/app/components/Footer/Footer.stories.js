import Footer from './Footer';

export default {
  component: Footer,
  title: 'Footer',
};

const Template = (args) => <Footer {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
