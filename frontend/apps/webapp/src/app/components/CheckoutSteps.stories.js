import CheckoutSteps from './CheckoutSteps';

export default {
  component: CheckoutSteps,
  title: 'CheckoutSteps',
};

const Template = (args) => <CheckoutSteps {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
