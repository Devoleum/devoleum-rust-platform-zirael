import StepScreen from './StepScreen';

export default {
  component: StepScreen,
  title: 'StepScreen',
};

const Template = (args) => <StepScreen {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
