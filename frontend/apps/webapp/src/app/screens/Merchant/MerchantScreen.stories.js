import MerchantScreen from './MerchantScreen';

export default {
  component: MerchantScreen,
  title: 'MerchantScreen',
};

const Template = (args) => <MerchantScreen {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
