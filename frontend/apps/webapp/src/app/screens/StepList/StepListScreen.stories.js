import StepListScreen from './StepListScreen';

export default {
  component: StepListScreen,
  title: 'StepListScreen',
};

const Template = (args) => <StepListScreen {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
